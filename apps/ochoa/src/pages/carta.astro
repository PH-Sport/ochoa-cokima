---
import { getCollection } from "astro:content";
import Base from "../layouts/Base.astro";
import MenuSection from "@tombo/ui/MenuSection.astro";

const all = (await getCollection("menu-es")).map((e) => e.data);
const bySection = (s: string) => all.filter((d) => d.section === s);
---

<Base
  title="La carta · Tasquita Los Ochoa"
  description="La carta de Los Ochoa en la Castellana: ensaladilla, croquetas de Joselito, bravas de feria, cachopín, croissant de rabo de toro y más. En ½ ración o ración."
  locale="es"
  altPath="/en/menu"
>
  <section class="carta">
    <div class="wrap">
      <p class="eyebrow">Tasquita · Madriz</p>
      <h1 class="cartel">Carta</h1>

      <MenuSection title="Para tapear" dishes={bySection("carta")} showAllergens={false} halfLabel="½ ración" fullLabel="Ración" layout="list" />
      <MenuSection title="Postres" dishes={bySection("postres")} showAllergens={false} layout="list" />

      <p class="note">Cesta de pan con aceite: 3 €. Carta de septiembre de 2025, la última publicada — pendiente de confirmar vigencia con el restaurante.</p>
    </div>
  </section>
</Base>

<style>
  .carta {
    padding: clamp(48px, 8vw, 92px) 0 0;
  }
  h1 {
    font-size: clamp(2.6rem, 7vw, 4.4rem);
    margin: 0.15rem 0 0;
  }
  .note {
    font-family: var(--sans);
    font-size: 0.88rem;
    color: var(--ink-faint);
    margin-top: 2.4rem;
    text-align: center;
  }
</style>

---
interface Props {
  locale: "es" | "en";
}
const { locale } = Astro.props;
const es = locale === "es";
---

<footer class="foot">
  <div class="wrap foot-in">
    <span class="brand cartel">Los Ochoa</span>
    <span>Castellana 117 · Madrid · <a href="tel:+34912876820">912 87 68 20</a></span>
    {/* En inglés se marca el idioma en el que están: el texto legal válido en
        España es el español y estas tres no se traducen, así que prometer
        «Legal notice» a secas y aterrizar en español se lee como un descuido.
        El «(ES)» avisa antes de pulsar. */}
    <span class="legal">
      <a href="/aviso-legal">{es ? "Aviso legal" : "Legal notice (ES)"}</a> ·
      <a href="/privacidad">{es ? "Privacidad" : "Privacy (ES)"}</a> ·
      <a href="/cookies">{es ? "Cookies" : "Cookies (ES)"}</a>
    </span>
  </div>
</footer>

<style>
  .foot {
    margin-top: clamp(52px, 8vw, 96px);
    border-top: 2px solid var(--ink);
    padding: 34px 0;
    background: var(--paper-2);
  }
  .foot-in {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    justify-content: space-between;
    align-items: center;
    font-family: var(--sans);
    font-size: 0.9rem;
    color: var(--ink-dim);
  }
  .brand {
    font-size: 1.15rem;
  }
  .legal a:hover {
    color: var(--rojo);
  }
</style>
